# Declared prior for `gemma4-assisted-v1`

These are the completed seed-42 random and evolutionary MGA archives used as
offline evidence by `config.llamacpp.assisted.example.json`. The corresponding
terminal manifests establish their configuration, completion state, and
resource accounting.

The adapter combines the two archive byte streams with an eight-byte
big-endian length prefix before each file. The expected combined SHA-256 is:

```text
874b7e4a6e9f6157bef916c6db6423f8f74d34c1d2095ccce19eb1d918dab40b
```

All 200 prior records—190 distinct body orders—are excluded from assisted
candidate menus. The files provide measured parents and aggregate score/cost
evidence; they are not counted among the assisted arm's 100 accepted routes.
